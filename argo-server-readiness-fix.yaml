spec:
  template:
    spec:
      containers:
      - name: argo-server
        args:
        - server
        - --access-control-allow-origin=*
        - --secure=false
        readinessProbe:
          httpGet:
            path: /
            port: 2746
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 20
